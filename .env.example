# Public URL of the deployed site, used for canonical URLs, sitemap and Open Graph.
NEXT_PUBLIC_SITE_URL=https://senova.studio

# Optional: endpoint that receives contact form submissions (e.g. Formspree, a Slack webhook proxy).
# When empty, /api/contact validates the payload and logs it on the server.
CONTACT_WEBHOOK_URL=
